# Used by `npm run build` (Vite's production mode), which is what the deploy runs.
# Takes precedence over .env, so local `npm run dev` keeps using the localhost backend.
#
# Vite inlines this at BUILD time - changing it on the host afterwards does nothing
# without a rebuild.
VITE_BACKEND_URL=https://ai-doc-backend-1.onrender.com
