# Base URL of the Spring Boot backend (no trailing slash).
#
# This file is the template for .env, which `npm run dev` reads.
# The deployed backend URL lives in .env.production, which `npm run build` reads instead.
VITE_BACKEND_URL=http://localhost:8080
